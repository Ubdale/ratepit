# Optional. Free key: https://fred.stlouisfed.org/docs/api/api_key.html
# Without it, the mortgage-rate endpoint falls back to a hardcoded estimate.
FRED_API_KEY=

# Used for canonical URLs / sitemap.
NEXT_PUBLIC_SITE_URL=https://ratepit.app
